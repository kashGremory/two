@model IEnumerable<two.Models.Livre>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LivreTitre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LivreAuteur)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LivreTitre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LivreAuteur)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LivreId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LivreId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LivreId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
